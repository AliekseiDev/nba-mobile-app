import validate from './validate';

export default function validateFormData(id) {
  this.setState((prevState) => {
    prevState.formData[id] = validate(prevState.formData[id]);
    return prevState;
  });
}