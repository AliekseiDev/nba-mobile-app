import React from 'react';

import style from '../articles.css';

/* === PROPS
team
*/
const TeamInfo = (props) => {
  return (
    <div className={style.articleHeader}>
      <div style={{backgroundImage: `url('/images/teams/${props.team.logo}')`}} className={style.articleHeader_left}></div>
      <div className={style.articleHeader_right}>
        <p>
          <span>{props.team.city} {props.team.name}</span>
        </p>
        <p>
          <strong>W{props.team.stats[0].wins}-L{props.team.stats[0].defeats}</strong>
        </p>
      </div>
    </div>
  );
};

export default TeamInfo;